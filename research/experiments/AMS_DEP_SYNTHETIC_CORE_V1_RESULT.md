# AMS-DEP Synthetic Numerical Core V1 — Calibration Result

## Classification

**SYNTHETIC NUMERICAL CALIBRATION — NOT MARKET EVIDENCE**

Final gate status: **FAIL — EMPIRICAL RELEASE BLOCKED**

This record preserves the first completed execution of the fixed
`AMS_DEP_SYNTHETIC_CALIBRATION_V1` design. It does not use BTC/ETH market
returns, Validation data, locked OOS data, strategy P&L, signals, or exchange
execution.

## Provenance

- Branch: `adaptive-markets-research`
- Frozen specification commit: `2dd1543f654b5a605a902ce2b3cf98c70c9755b1`
- Completed executing commit: `a6caa1d2516d76fc313f16e20719d61749f83456`
- Workflow: `AMS DEP Synthetic Core V1`
- Completed synthetic run: `35434647946`
- Artifact: `ams-dep-synthetic-core-v1`
- Artifact ID: `10582128182`
- Artifact digest: `sha256:29485cf1e4102245b3b1f92fe1900d77f79ce1564f3cebf16dce276e3efef36b`
- Replications: 1,000 per case × 7 fixed cases = 7,000
- Full repository regression-suite step: PASS
- Numerical-core execution step: PASS
- Predeclared engineering-screen enforcement: FAIL
- Artifact upload: PASS

The preceding run `35434599273` at commit
`a7da36634fd194dd621cd95f26ecffa6e5680de7` failed before simulation because
the workflow used a shallow checkout and the runner could not inspect the
already-frozen specification commit. No synthetic result was produced by that
run. The only correction was workflow checkout history (`fetch-depth: 0`);
the synthetic design, DGPs, seeds, replication counts, statistical code,
thresholds, and frozen specification were unchanged.

## Frozen screening criteria

The preregistered engineering screen required:

- null-family erroneous-rejection rate <= 0.075;
- invalid-fit fraction <= 0.01;
- strong-alternative power >= 0.80 separately for the registered asset/test
  targets;
- no early stopping, case deletion, seed rerolling, or threshold changes.

A failure remains a failure; these criteria are not modified after the result.

## Results

All seven cases completed 1,000 replications. Invalid-fit rate was 0 in every
case.

| Synthetic case | Any-true-null erroneous rejection | Registered power result | Screen |
| --- | ---: | --- | --- |
| `iid_null` | 0.126 (126/1000) | n/a | **FAIL** |
| `heteroskedastic_null` | 0.247 (247/1000) | n/a | **FAIL** |
| `garch_null` | 0.143 (143/1000) | n/a | **FAIL** |
| `stable_ar` | 0.116 (116/1000) for true TIME/STATE nulls | BTC DEP 1.000; ETH DEP 1.000 | **FAIL** |
| `time_ar` | 0.075 (75/1000) for true STATE nulls | BTC TIME 1.000; ETH TIME 1.000 | PASS |
| `state_ar` | 0.123 (123/1000) for true TIME nulls | BTC STATE 1.000; ETH STATE 1.000 | **FAIL** |
| `bid_ask_bounce` | no null-size requirement by design | measured DEP 1.000 on both assets; classified measurement effect | PASS for its registered purpose |

Selected per-slot rejection rates under null cases also show that the problem
is not merely one isolated family event. In `heteroskedastic_null`, for
example, BTC DEP rejected in 12.7% of replications, ETH DEP in 11.2%, BTC TIME
in 9.4%, and ETH TIME in 8.8%. The fixed six-slot Holm procedure therefore did
not achieve the preregistered family-level engineering bound under the
registered synthetic conditions.

## Interpretation

The synthetic core demonstrates high detection power for the planted stable,
time-varying, and state-varying dependence alternatives, but the current
asymptotic HAC/Wald inference is not sufficiently calibrated under the frozen
synthetic nulls.

This is a statistical-calibration failure, not evidence about BTC, ETH,
Adaptive Markets, or tradability. It is exactly the type of false-positive
risk the synthetic gate was designed to detect before empirical inspection.

The bid/ask-bounce case also behaves as intended as a caution: the procedure
detects dependence generated by a measurement/microstructure mechanism even
though the latent efficient-price innovations are independent. Detection of
serial dependence must therefore not be equated with an economic trading edge.

## Decision

**EMPIRICAL RELEASE REMAINS BLOCKED.**

Do not run `AMS-DEP-001-v1` on actual BTCUSDT/ETHUSDT returns.

Do not:

- weaken the 0.075 null-family ceiling;
- remove difficult null cases;
- reroll seeds;
- reduce the six-test multiplicity family;
- reinterpret the failed screen as acceptable because power is high;
- access Validation or locked OOS;
- generate strategy P&L or trading signals.

Any revised inference method is a new, prospectively specified calibration
version. The V1 result and artifact must remain retained. A future version
must state the statistical rationale for the change before new synthetic
results are observed and must pass its own fixed calibration gate before any
empirical-release review.

## Research boundary

- Market data accessed by this calibration: **NO**
- Validation/OOS accessed: **NO**
- Strategy P&L calculated: **NO**
- Strategy signals generated: **NO**
- Empirical release authorized: **NO**
- Paper/live trading authorized: **NO**

## Retained local execution and reconciliation (2026-09-19)

A separately completed local execution at the original implementation commit
`7bd395d53a6cb456949049e3c1ddbcd7b26bc5e2` reproduced every case-level
false-rejection count, registered power outcome and screen decision reported
above for CI run `35434647946`. It used the same frozen specification, code,
DGPs and seed schedule. This is reproducibility evidence from the same
researcher, **not an independent statistical approval or an additional
independent Monte Carlo sample**. No inferential claim is strengthened by
counting these executions twice.

The local machine-readable outputs are retained in Git to supplement the
expiring CI artifact:

- [Summary and provenance](ams_dep_synthetic_core_v1_results/summary.json)
- [All 7,000 replicate records](ams_dep_synthetic_core_v1_results/replicates.jsonl.gz)
- Compressed ledger SHA-256:
  `4b0f03574641add65089494dd41ee8195aae03e160759bcb8e2889320908fb0b`
- Uncompressed ledger SHA-256:
  `08ebef6ea76866fc8214132e45077b80a59266e8be625e642a89451e728467c4`

Both ledger hashes and the 7,000-record count were verified. The local
summary records Python 3.12.14, NumPy 2.2.6, SciPy 1.15.3 and one BLAS
thread, plus SHA-256 hashes for the executing sources and frozen documents.
The ledger's Holm decisions and aggregate counts were also recomputed by
separate bookkeeping code; this is an author check, not independent review.

Before adding these outputs, the remote branch was reconciled at
`8989ede4611615283c29bc0344567bdd8cb2bac1`. Its completed failure diagnostic,
V2 literature/review packet, Issue #44, and machine-readable release lock
remain authoritative and unchanged. The current full suite passed all 169
tests with `PYTHONPATH=src python -m pytest -q`. An initial worktree test
invocation failed collection because an editable environment pointed to the
older checkout; selecting the reconciled source tree resolved this without
code changes. GitHub release-gate run `35437149230` also passed at that
reconciled commit; it verifies that execution remains blocked, not that a
method is approved.

**V1 remains failed. V2 synthetic execution and empirical execution remain
blocked pending the review and release sequence in Issue #44 and
`research/governance/ams_dep_release_gate_v1.json`.** No market data,
Validation/OOS, P&L or trading was accessed by this local execution. The
closed OHLCV program remains closed with no promoted candidate.
