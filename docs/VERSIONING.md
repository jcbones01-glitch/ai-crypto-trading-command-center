# Versioning

Code is versioned with Git. Strategies use explicit IDs and versions, for example `S-001 V0.1` and `S-001 V0.2`.

Research records link hypothesis IDs, strategy IDs/versions, experiments, datasets, code identifiers, assumptions, results, conclusions, and decisions.

A change must create a traceable new version rather than silently changing a prior research object.
